# Prompts

Prompt files are not active in this MVP slice because model workflows are not implemented yet.

Future prompt packs should be versioned by capability:

- `scene_planner`
- `scene_writer`
- `continuity_reviewer`
- `memory_curator`

Every AI output must store the prompt snapshot and context manifest before it can be shown as a draft.
